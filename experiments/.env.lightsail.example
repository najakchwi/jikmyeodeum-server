# 로컬 전용 파일(server/experiments/.env.lightsail)로 복사해 채운다.
LIGHTSAIL_SSH_HOST=jikmyeodeum
LIGHTSAIL_REMOTE_DIR=/home/ubuntu

# 로컬 부하 생성기와 지표 수집기가 호출할 실험 서버 주소
EXPERIMENT_API_BASE_URL=https://{LIGHTSAIL_DOMAIN}
